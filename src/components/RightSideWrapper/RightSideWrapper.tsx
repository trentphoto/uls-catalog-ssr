import React from 'react'
import './RightSideWrapper.css'

const RightSideWrapper: React.SFC = props => (
  <div className="RightSideWrapper">{props.children}</div>
)

export default RightSideWrapper
